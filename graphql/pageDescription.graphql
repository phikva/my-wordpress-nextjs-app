query GetPageDescription($pageId: ID!) {
  page(id: $pageId) {
    id
     template {
      ... on DefaultTemplate {
        templateName
        pageDescription {
          description
        }
      }
    }
  }
}